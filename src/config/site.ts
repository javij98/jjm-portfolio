/** Cambia este valor para mostrar u ocultar el blog en toda la web. */
export const BLOG_ENABLED = true;

/** Número de proyectos visibles en la portada antes de «ver todos». */
export const HOME_PROJECT_PREVIEW_LIMIT = 3;
